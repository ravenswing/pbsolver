[project]
name = "pbsolver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.7",
    "mdanalysis>=2.7.0",
]

[tool.uv]
python-preference = "managed"
